<%= simple_form_for([:admin, @studiyear]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :even_odd, as: :checkbox %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
